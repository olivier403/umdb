# Backend

Spring Boot (Java 21) API with Postgres + pgvector.
Config: `src/main/resources/application.yml` (override via env).
Config: disable review writes with `APP_REVIEWS_ENABLED=false` (defaults to `true`).
See root `README.md` for run/test instructions.
