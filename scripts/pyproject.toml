[project]
name = "scripts"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "polars>=1.37.1",
    "requests>=2.32.5",
    "sentence-transformers>=3.4.1",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
